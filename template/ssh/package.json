{
    "devDependencies": {
        "scp2": "^0.5.0",
        "archiver": "^6.0.1"
    },
    "scripts": {
        "deploy": "npm run build && node ./scripts/zip.js && node ./scripts/push.js"
      }
}