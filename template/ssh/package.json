{
    "devDependencies": {
        "scp2": "^0.5.0",
        "archiver": "^6.0.1"
    },
    "scripts": {
        "push": "npm run build && node ./config/zip.js && node ./config/push.js"
      }
}